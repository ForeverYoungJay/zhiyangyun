# 权限矩阵（当前版）

> 当前实现状态：**基础账号鉴权已生效；细粒度 RBAC 待下一轮落地**。

## 建议矩阵（目标态）
- 超级管理员：全模块
- 院长：A1/A2/B 可读写 + 审核
- 财务：M7 + B2账单 + 报表
- 护理员：M3任务执行、查看本人绩效
- 家属：仅 B2 自关联长者数据

## 本轮前端入口
- B2 增加“总览/服务增购”入口，仍依赖统一登录态。

## 下轮落地任务
1. `CurrentUser` 注入角色与权限集合
2. 路由级 `require_permission()`
3. 关键列表增加 tenant+role data-scope
